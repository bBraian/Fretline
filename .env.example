# VITE_ASSETS_BASE mora só nas variáveis do projeto na Vercel (como Config).
# Aqui fica vazia: com ela, `npm run dev` e os scripts de navegador leem a
# biblioteca do host em vez de songs/. Ver a seção Hospedagem do CLAUDE.md.
VITE_ASSETS_BASE=
